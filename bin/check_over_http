#!/usr/bin/perl

use 5.008001;
use strict;
use utf8;
use version 0.74;
use warnings 'all';

use Nagios::Plugin::OverHTTP;

# Script metadata
our $AUTHORITY = 'cpan:DOUGDUDE';
our $VERSION   = '0.04';

# Make a plugin instance, run, and exit
exit Nagios::Plugin::OverHTTP->new_with_options->run;
